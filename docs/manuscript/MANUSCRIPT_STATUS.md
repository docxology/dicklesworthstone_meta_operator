# Manuscript Status — dicklesworthstone_meta_operator

Live status of the artifact-backed manuscript. This file is hand-maintained
(below) but every *number* it cites is a generated token.

| Surface | Status |
| --- | --- |
| Token discipline (prose ↔ generator, both directions) | enforced by `tests/test_manuscript_variables.py` |
| Figures (4) | generated by `scripts/65_generate_figures.py`, deterministic |
| Gate coverage of manuscript artifacts | `figures_present` + `manuscript_tokens_present` checks in `src/health_gate.py` |
| PDF render | via the template pipeline (`scripts/pipeline/stage_03_render.py --project ongoing/Code_Tools/dicklesworthstone_meta_operator`), attempted during initial build |

## Notes

- The captured numbers are a snapshot of a live corpus; the daily refresh
  regenerates artifacts, figures, tokens, and gate state. The token test keeps
  prose and evidence in lockstep across those refreshes.
- Prose deliberately discusses the NO-GO tail (live upstream velocity) as a
  finding, not a bug; do not "fix" the manuscript to claim a static GO.
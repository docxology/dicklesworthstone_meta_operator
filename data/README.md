# data/

Versioned inputs: [`operator_config.yaml`](operator_config.yaml) is the single
runtime configuration (target GitHub user, clone/workers budget, timeouts).
Everything generated lives under `output/`. See [`AGENTS.md`](AGENTS.md).
